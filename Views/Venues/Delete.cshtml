@model Venue
@{
    ViewData["Title"] = $"Delete {Model.Name}";
}

<section class="form-page form-page--delete">
    <div class="form-header">
        <h1>Delete Venue</h1>
        <p class="section-copy warning">This action cannot be undone. Please confirm before deleting.</p>
    </div>

    <article class="content-card form-card form-card--danger">
        <div class="delete-confirmation">
            <h2>Are you sure?</h2>
            <p>You are about to delete the following venue:</p>
            
            <div class="delete-info">
                <p><strong>Name:</strong> @Model.Name</p>
                <p><strong>Location:</strong> @Model.Address, @Model.City</p>
                <p><strong>Capacity:</strong> @Model.Capacity people</p>
                <p><strong>Availability:</strong> @(Model.IsAvailable ? "Available" : "Not Available")</p>
            </div>

            <form asp-action="Delete" method="post" class="delete-form">
                <div class="form-actions">
                    <button type="submit" class="btn btn-danger">Confirm Delete</button>
                    <a asp-action="Details" asp-route-id="@Model.Id" class="btn btn-secondary">Cancel</a>
                </div>
            </form>
        </div>
    </article>
</section>
