@model Team
@{
    ViewData["Title"] = $"Delete {Model.Name}";
}

<section class="form-page form-page--delete">
    <div class="form-header">
        <h1>Delete Team</h1>
        <p class="section-copy warning">This action cannot be undone. Please confirm before deleting.</p>
    </div>

    <article class="content-card form-card form-card--danger">
        <div class="delete-confirmation">
            <h2>Are you sure?</h2>
            <p>You are about to delete the following team:</p>

            <div class="delete-info">
                <p><strong>Name:</strong> @Model.Name</p>
                <p><strong>Coach:</strong> @(Model.Coach?.Name ?? "—")</p>
                <p><strong>Manager:</strong> @(Model.Manager?.Name ?? "—")</p>
                <p><strong>Registered:</strong> @Model.RegisteredAt.ToString("dd MMM yyyy")</p>
                <p><strong>Roster Confirmed:</strong> @(Model.IsRosterConfirmed ? "Yes" : "No")</p>
            </div>

            <form asp-action="Delete" method="post" class="delete-form">
                @Html.AntiForgeryToken()
                <div class="form-actions">
                    <button type="submit" class="btn btn-danger">Confirm Delete</button>
                    <a asp-action="Details" asp-route-id="@Model.Id" class="btn btn-secondary">Cancel</a>
                </div>
            </form>
        </div>
    </article>
</section>
